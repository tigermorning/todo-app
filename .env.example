# 이 파일을 .env로 복사한 뒤 값을 채우세요. .env는 git에 커밋하지 마세요.

# Google Cloud Console에서 만든 OAuth 클라이언트(데스크톱 앱)의 credentials.json 경로
GOOGLE_CREDENTIALS_FILE=credentials.json

# 최초 로그인 후 자동으로 생성/갱신되는 토큰 저장 위치 (직접 만들 필요 없음)
GOOGLE_TOKEN_FILE=token.json

# 며칠 앞 일정까지 "제안된 할 일"로 보여줄지
CALENDAR_SUGGESTION_DAYS=14
